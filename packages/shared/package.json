{
  "name": "@nexora/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Nexora — shared types, constants and niche configs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
